{
  "name": "knox-knows",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Knox Knows — AI homework companion",
  "author": "Sion",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "test:source": "node scripts/check-source.mjs",
    "test": "node scripts/check-source.mjs && node scripts/check-behavior.mjs"
  },
  "dependencies": {
    "stripe": "^14.21.0",
    "firebase-admin": "^12.1.0"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}
